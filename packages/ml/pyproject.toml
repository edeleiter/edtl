[project]
name = "ml"
version = "0.1.0"
description = "ML training, evaluation, and inference"
requires-python = ">=3.11"
dependencies = [
    "schemas",
    "transforms",
    "backends",
    "nfl",
    "scikit-learn>=1.4.0",
    "xgboost>=2.0.0",
    "pandas>=2.0.0",
    "numpy>=1.26.0",
    "joblib>=1.3.0",
    "ibis-framework[duckdb]>=9.0.0",
]

[tool.uv.sources]
schemas = { workspace = true }
transforms = { workspace = true }
backends = { workspace = true }
nfl = { workspace = true }
